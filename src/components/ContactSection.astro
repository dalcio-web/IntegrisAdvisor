<section class="contact" id="contato">
  <div class="contact__inner">
    <div class="contact__copy" data-node-id="170:10">
      <h2 data-reveal data-reveal-delay="0">Pronto para blindar o seu negócio?</h2>
      <p data-reveal data-reveal-delay="110">
        Agende uma conversa estratégica gratuita de 30 minutos com nossa equipe de especialistas.
        <strong>Vamos analisar o momento atual da sua empresa e indicar o plano ideal para a sua segurança.</strong>
      </p>
    </div>

    <form
      class="contact-form"
      data-node-id="72:935"
      data-reveal
      data-reveal-from="right"
      data-reveal-delay="150"
      style="min-height: 0;"
    >
      <div class="contact-form__fields">
        <label data-reveal data-reveal-delay="280">
          <span>Nome</span>
          <input type="text" name="name" placeholder="Nome:" autocomplete="name" required />
        </label>
        <label data-reveal data-reveal-delay="360">
          <span>E-mail</span>
          <input type="email" name="email" placeholder="E-mail:" autocomplete="email" required />
        </label>
        <label data-reveal data-reveal-delay="440">
          <span>Telefone</span>
          <input type="tel" name="phone" placeholder="Telefone:" autocomplete="tel" required />
        </label>
        <label data-reveal data-reveal-delay="520">
          <span>Nome da empresa</span>
          <input type="text" name="company" placeholder="Nome da empresa:" autocomplete="organization" />
        </label>
      </div>

      <button type="submit" data-reveal data-reveal-delay="610">Solicitar Minha Reunião Gratuita</button>
    </form>
  </div>
</section>

<script>
  document.querySelector<HTMLFormElement>('.contact-form')?.addEventListener('submit', (event) => {
    event.preventDefault();
  });
</script>

<style>
  .contact {
    position: relative;
    z-index: 2;
    min-height: 862px;
    margin-top: -48px;
    padding: 185px 24px 90px;
    background-color: var(--color-black);
    background-image: url('/assets/BG-HERO-SECTION.png');
    background-position: center;
    background-repeat: no-repeat;
    background-size: 100% 100%;
    scroll-margin-top: 80px;
  }

  .contact::after {
    position: absolute;
    z-index: 0;
    inset: 0;
    background: linear-gradient(180deg, rgba(0, 0, 0, .08), rgba(0, 0, 0, .46));
    pointer-events: none;
    content: '';
  }

  .contact__inner {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 490px 565px;
    gap: 125px;
    width: min(100%, 1180px);
    margin: 0 auto;
  }

  .contact__copy { padding-top: 68px; }

  .contact__copy h2 {
    width: 490px;
    margin: 0;
    font-family: var(--font-display);
    font-size: 56px;
    font-weight: 600;
    line-height: 1.15;
    letter-spacing: -1.68px;
  }

  .contact__copy p {
    width: 486px;
    margin: 46px 0 0;
    font-size: 20px;
    line-height: 1.55;
  }

  .contact__copy strong {
    display: block;
    margin-top: 31px;
    color: #d6a73e;
    font-weight: 700;
  }

  .contact-form {
    display: flex;
    width: 565px;
    min-height: 0;
    flex-direction: column;
    gap: 55px;
    padding: 46px 45px;
    border: 1px solid #d6a73e;
    border-radius: 25px;
    background: rgba(255, 255, 255, .1);
  }

  .contact-form__fields { display: flex; flex-direction: column; gap: 15px; }
  label { display: block; }
  label span { position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0, 0, 0, 0); }

  input {
    width: 100%;
    height: 50px;
    padding: 0 16px;
    border: 1px solid #d6a73e;
    border-radius: 5px;
    outline: none;
    background: #fff;
    color: rgba(0, 0, 0, .85);
    font: 400 12px/1.55 var(--font-body);
  }

  input::placeholder { color: rgba(0, 0, 0, .6); opacity: 1; }
  input:focus { border-color: var(--color-gold-light); box-shadow: 0 0 0 3px rgba(200, 140, 29, .25); }

  button {
    width: 100%;
    min-height: 59px;
    border: 1px solid #090500;
    border-radius: 45px;
    background: linear-gradient(90deg, var(--color-gold-dark), var(--color-gold) 50%, var(--color-gold-light));
    box-shadow: inset 0 0 7px rgba(255, 255, 255, .35);
    color: #fff;
    font-size: 16px;
    font-weight: 600;
    cursor: pointer;
    transition: filter .2s ease, transform .2s ease;
  }

  button:hover { filter: brightness(1.06); transform: translateY(-1px); }
  button:focus-visible { outline: 2px solid var(--color-gold-light); outline-offset: 4px; }

  @media (max-width: 1230px) {
    .contact { padding-top: 130px; }
    .contact__inner { grid-template-columns: 1fr; gap: 64px; max-width: 620px; }
    .contact__copy { padding-top: 0; }
    .contact__copy h2, .contact__copy p, .contact-form { width: 100%; }
  }

  @media (max-width: 620px) {
    .contact { min-height: auto; padding: 110px 16px 80px; background-size: auto 100%; }
    .contact__copy h2 { font-size: 42px; }
    .contact__copy p { font-size: 17px; }
    .contact-form { min-height: auto; gap: 36px; padding: 30px 20px; }
  }
</style>
